 <form action="/entry/authenticate" method="POST">
                    <span>{{form.message}}</span>
                    <div>
                        <input name="username" maxlength="255" type="text" placeholder="Имя пользователя*" value="{{form.username}}">
                    </div>
                    <div class="form-group">
                        <input name="password" type="password" placeholder="Пароль*">
                    </div>
                    <button id="authenticate_sumbit" type="submit">Войти</button>
                </form>